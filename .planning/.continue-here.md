---
context: phase
phase: between-phases (Phase 1 complete, Phase 2 not started)
task: complete
total_tasks: complete
status: between-phases
last_updated: 2026-04-28
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. Phase 1 completed cleanly with no methodological failures._

## Critical Anti-Patterns

_None identified this session._

<current_state>
Phase 1 (Codebase Cleanup) is fully complete and verified. All 6 requirements CLEAN-01 through CLEAN-06 are satisfied. The working tree is clean.

We are at the transition point between Phase 1 and Phase 2.

Phase 2 is Security Hardening — it modifies **live endpoints** that active testers are using. This is the riskiest phase of the milestone and requires careful testing before deploy.
</current_state>

<completed_work>

**Phase 1: Codebase Cleanup — ALL PLANS COMPLETE**

- **01-01** (CLEAN-01, CLEAN-02, CLEAN-04): Deleted chat_endpoint_patch.py, consolidated_patch.py, outlook_integration.py, root index.html — 1,362 lines removed. Live imports confirmed absent before deletion.
- **01-02** (CLEAN-03): Inspected files.zip via `unzip -l` — SAFE (source code snapshot only: main.py, calendar_integration.py, outlook_integration.py, drive_integration.py, requirements.txt). Removed via normal git rm (PATH A). No history purge, no force-push required.
- **01-03** (CLEAN-05): Replaced hardcoded model literals in note_tidy.py (was `"claude-sonnet-4-5"`) and email_watcher.py (was `"claude-sonnet-4-6"`) with `os.getenv("CLAUDE_MODEL", "claude-sonnet-4-5")`.
- **01-04** (CLEAN-06): Added 7 credential patterns to .gitignore (*.token, *.key, *.pem, *.p12, token.json, google_token.json, credentials.json). Git history scan: **CLEAN** — no credentials ever committed.

**Verification:** Passed — 4/4 must-haves confirmed against live codebase.

**Code Review:** 7 findings (1 critical, 4 warnings, 2 info) — all pre-existing bugs, none introduced by Phase 1.
</completed_work>

<remaining_work>

**Phase 2: Security Hardening (not yet planned)**
- SEC-01: Add Bearer token middleware to all /api/* routes
- SEC-02: Restrict CORS to ALLOWED_ORIGIN env var (currently wildcard *)
- SEC-03: Fix /auth/google/callback to not render token in HTML
- SEC-04: Rotate Google OAuth credentials (HUMAN ACTION REQUIRED — see below)

**Optional pre-Phase-2 work:**
- Fix CR-01 pre-existing bug: `body_start = 0` in note_tidy.py:147 → run `/gsd-code-review-fix 1`
</remaining_work>

<decisions_made>

- **files.zip: SAFE** — source code snapshot only, no sensitive data. PATH A (normal git rm) taken. No force-push to remote required.
- **Model fallback: claude-sonnet-4-5** — normalised email_watcher.py default from 4-6 to 4-5 to match main.py; Railway CLAUDE_MODEL env var controls actual model at runtime.
- **Git history: clean** — no credentials ever committed. SEC-04 rotation is precautionary (prior browser UI exposure) not an emergency purge.
</decisions_made>

<blockers>

**Human action required before or during Phase 2:**
- **SEC-04 credential rotation**: Google OAuth credentials (client secret, access token, refresh token) must be rotated in Google Cloud Console because the token was previously visible in the browser UI at /auth/google/callback. New values must be set in Railway env vars. This is a Phase 2 task but the rotation itself requires manual action in Google Cloud Console.

**No other blockers.** Working tree is clean.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current position and accumulated decisions
2. `.planning/ROADMAP.md` — Phase 2 goal, requirements (SEC-01 through SEC-04), and success criteria
3. `.planning/phases/01-codebase-cleanup/01-REVIEW.md` — code review findings (advisory; CR-01 is the most significant pre-existing bug)
4. `main.py` — before touching any Phase 2 security changes; this is the entry point with all routes and current CORS config

## Critical Anti-Patterns (do NOT repeat these)

_None encountered this session._

## Infrastructure State

- **Railway deployment**: live with active testers — any change to auth or chat endpoint is immediately felt
- **Google integration**: OAuth token previously exposed in callback page (rotation is Phase 2 SEC-04)
- **No running local servers**: development is edit → commit → Railway auto-deploy
- **Python runtime**: Python is not available in the bash shell on this Windows machine (`python3` exits 49). Use `python` via Windows Store or test via Railway deployment. The smoke-test in 01-03 was skipped for this reason — the edits were trivial and verified via grep.

<context>
Phase 1 was a clean, risk-free phase — pure deletions and minor text edits with no running code touched. The session went smoothly with one human checkpoint (files.zip inspection).

Phase 2 is the dangerous one. It modifies live endpoints with active testers. The plan needs to be careful about:
1. Not breaking the existing JS frontend when adding auth middleware
2. Testing the 401 flow before deploying
3. SEC-04 rotation timing — if you rotate creds before SEC-03 (callback fix), the new token will also be exposed in the browser. Do SEC-03 first, then SEC-04.

The code review found CR-01 (flag parser bug in note_tidy.py) which is unrelated to security but worth fixing. Running `/gsd-code-review-fix 1` before Phase 2 is recommended but not blocking.
</context>

<next_action>
Start with: `/gsd-discuss-phase 2` — discuss and capture design decisions for Phase 2: Security Hardening before planning.

Key decision to capture in discuss-phase: SEC-03 must be implemented BEFORE SEC-04 (don't rotate creds while the callback still renders them in the browser).

If credential rotation has already happened: skip to `/gsd-plan-phase 2`.
</next_action>
