---
phase: 01-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chat_endpoint_patch.py
  - consolidated_patch.py
  - outlook_integration.py
  - index.html
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-04
must_haves:
  truths:
    - "chat_endpoint_patch.py is absent from the working tree and from git ls-files"
    - "consolidated_patch.py is absent from the working tree and from git ls-files"
    - "outlook_integration.py is absent from the working tree and from git ls-files"
    - "Root index.html is absent from the working tree and from git ls-files"
    - "static/index.html still exists and is untouched"
  artifacts:
    - path: "chat_endpoint_patch.py"
      provides: "DELETED — must NOT exist"
    - path: "consolidated_patch.py"
      provides: "DELETED — must NOT exist"
    - path: "outlook_integration.py"
      provides: "DELETED — must NOT exist"
    - path: "index.html"
      provides: "DELETED — must NOT exist (root only; static/index.html is preserved)"
  key_links:
    - from: "main.py imports"
      to: "chat_endpoint_patch, consolidated_patch, outlook_integration"
      via: "import statements"
      pattern: "import.*patch|import.*outlook"
---

<objective>
Delete four confirmed-dead files from the repository so the working tree contains only live, intentional code.

Purpose: Eliminate 42 KB of stale patches and a dead Outlook integration that add confusion and maintenance surface with no runtime value. Also remove the root index.html duplicate so there is one canonical UI file.
Output: Four files permanently removed via `git rm`, committed to history. No running code is modified.
</objective>

<execution_context>
@C:/Users/ralco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ralco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no live imports, then delete dead patch and integration files</name>
  <read_first>
    - main.py (full file — confirm no import of chat_endpoint_patch, consolidated_patch, or outlook_integration)
    - chat_endpoint_patch.py (skim to confirm it is patch/dead code)
    - consolidated_patch.py (skim to confirm it is patch/dead code)
    - outlook_integration.py (skim to confirm Outlook/Microsoft Graph only — no shared utilities)
  </read_first>
  <files>chat_endpoint_patch.py, consolidated_patch.py, outlook_integration.py</files>
  <action>
    1. Run: `grep -rn "chat_endpoint_patch\|consolidated_patch\|outlook_integration" /c/Users/ralco/Downloads/midwife-agent/ --include="*.py" --exclude-dir=.git`
       Expected result: zero matches in any file other than the files themselves. If any live import is found, stop and report before deleting.
    2. If no live imports found, delete using git rm:
       ```
       git -C /c/Users/ralco/Downloads/midwife-agent rm chat_endpoint_patch.py consolidated_patch.py outlook_integration.py
       ```
    3. Verify the three files no longer appear in `git ls-files`.
  </action>
  <verify>
    <automated>git -C /c/Users/ralco/Downloads/midwife-agent ls-files chat_endpoint_patch.py consolidated_patch.py outlook_integration.py</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files chat_endpoint_patch.py` returns empty string (exit 0, no output)
    - `git ls-files consolidated_patch.py` returns empty string (exit 0, no output)
    - `git ls-files outlook_integration.py` returns empty string (exit 0, no output)
    - `grep -rn "from outlook_integration\|import outlook_integration" . --include="*.py"` returns zero matches
    - `grep -rn "from chat_endpoint_patch\|import chat_endpoint_patch\|from consolidated_patch\|import consolidated_patch" . --include="*.py"` returns zero matches
  </acceptance_criteria>
  <done>Three dead files are removed from the index. No live imports remain in any Python file.</done>
</task>

<task type="auto">
  <name>Task 2: Delete root index.html duplicate</name>
  <read_first>
    - index.html (root — confirm it is a stale duplicate, not the canonical UI)
    - static/index.html (confirm it exists and is the canonical file)
  </read_first>
  <files>index.html</files>
  <action>
    1. Confirm `static/index.html` exists: `ls /c/Users/ralco/Downloads/midwife-agent/static/index.html`
    2. Confirm root `index.html` is a duplicate (different mtime or content from static/index.html — do not need byte-identical, just confirm it is not the live file served by FastAPI).
       The FastAPI app mounts `StaticFiles(directory=static_dir)` where `static_dir = Path(__file__).parent / "static"`. The root index.html is NOT served by this mount, confirming it is unused.
    3. Delete: `git -C /c/Users/ralco/Downloads/midwife-agent rm index.html`
    4. Verify: `git ls-files index.html` returns empty.
  </action>
  <verify>
    <automated>git -C /c/Users/ralco/Downloads/midwife-agent ls-files index.html && ls /c/Users/ralco/Downloads/midwife-agent/static/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files index.html` at repo root returns empty string (file removed from index)
    - `ls static/index.html` returns the file path with no error (canonical UI preserved)
    - `git ls-files static/index.html` returns `static/index.html` (canonical UI still tracked)
  </acceptance_criteria>
  <done>Root index.html removed from git index. static/index.html is untouched and still tracked.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the deletions</name>
  <read_first>
    - Run `git status` to confirm exactly the four files are staged for deletion and nothing else
  </read_first>
  <files>(git commit only — no source files modified)</files>
  <action>
    1. Confirm staging area: `git -C /c/Users/ralco/Downloads/midwife-agent status`
       Expected: four deletions staged (chat_endpoint_patch.py, consolidated_patch.py, outlook_integration.py, index.html). Nothing else staged.
    2. If staging area is correct, commit:
       ```
       git -C /c/Users/ralco/Downloads/midwife-agent commit -m "chore(clean): delete dead patch files, outlook integration, root index.html duplicate (CLEAN-01, CLEAN-02, CLEAN-04)"
       ```
    3. Confirm: `git log --oneline -1` shows the new commit message.
  </action>
  <verify>
    <automated>git -C /c/Users/ralco/Downloads/midwife-agent log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` contains the string "CLEAN-01"
    - `git show --stat HEAD` lists exactly these four files as deleted: `chat_endpoint_patch.py`, `consolidated_patch.py`, `outlook_integration.py`, `index.html`
    - `git show --stat HEAD` shows no other files modified
  </acceptance_criteria>
  <done>Deletions committed. Git history is clean with a traceable commit message referencing requirement IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → working tree | Deletion of files; no network traffic, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | git rm of wrong files | mitigate | Task 1 explicitly checks for live imports before deletion; executor stops and reports if any import found |
| T-01-01-02 | Information Disclosure | outlook_integration.py may contain embedded credentials | mitigate | Skim file in read_first step before deletion; if credentials found, report before committing so SEC-04 / git history purge can be triggered |
| T-01-01-03 | Denial of Service | Deleting static/index.html by mistake (wrong path) | mitigate | Task 2 explicitly verifies static/index.html exists before and after deletion of root index.html |

N/A — this plan does not modify any running code. No endpoints, middleware, or application logic are touched.
</threat_model>

<verification>
After all three tasks complete:

```bash
# All four files absent from index
git ls-files chat_endpoint_patch.py consolidated_patch.py outlook_integration.py index.html
# Expected: empty output

# Canonical UI still present
git ls-files static/index.html
# Expected: static/index.html

# No dead imports remain
grep -rn "outlook_integration\|chat_endpoint_patch\|consolidated_patch" . --include="*.py"
# Expected: zero matches
```
</verification>

<success_criteria>
- `git ls-files` shows none of the four deleted files
- `git ls-files static/index.html` shows the canonical UI is intact
- No Python file imports any of the deleted modules
- Commit exists with message referencing CLEAN-01, CLEAN-02, CLEAN-04
</success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-cleanup/01-01-SUMMARY.md` using the summary template.
</output>
