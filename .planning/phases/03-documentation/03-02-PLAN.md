---
phase: 03-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "A developer can read README.md and know exactly what every environment variable does and where to get its value"
    - "README.md covers all 11 required env vars with no gaps"
    - "README.md provides enough quick-start context to run the app locally"
  artifacts:
    - path: "README.md"
      provides: "Environment variable reference table (11 vars), quick-start section"
      contains: "API_KEY"
  key_links:
    - from: "README.md"
      to: "main.py"
      via: "documents env vars read by main.py"
      pattern: "RAILWAY_PUBLIC_DOMAIN"
    - from: "README.md"
      to: "SECURITY.md"
      via: "references SECURITY.md for auth details"
      pattern: "SECURITY.md"
---

<objective>
Create README.md at the repo root with a quick-start section and a complete environment variable reference covering all 11 required vars.

Purpose: A developer or operator picking up this codebase must be able to set up the application and understand every required configuration value without reading source code.

Output: README.md at repo root. The file does not exist yet. It must contain: project description, quick-start instructions, and a complete env var table.
</objective>

<execution_context>
@C:/Users/ralco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ralco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- All env vars extracted from source. Executor writes README.md using these facts. No source exploration needed. -->

All env vars and their locations:

| Var | File | Line/Pattern | Default | Required? |
|-----|------|--------------|---------|-----------|
| API_KEY | main.py:59 | os.getenv("API_KEY", "") | (none — fail-closed) | Yes — all /api/* return 401 without it |
| ALLOWED_ORIGIN | main.py:47 | os.getenv("ALLOWED_ORIGIN", "http://localhost:8000") | http://localhost:8000 | Yes for production |
| CLAUDE_MODEL | main.py:58, email_watcher.py:93, note_tidy.py:131 | os.getenv("CLAUDE_MODEL", "claude-sonnet-4-5") | claude-sonnet-4-5 | No |
| ANTHROPIC_API_KEY | main.py:500, email_watcher.py:49, note_tidy.py:121 | os.getenv("ANTHROPIC_API_KEY") | (none) | Yes |
| GOOGLE_CLIENT_ID | calendar_integration.py:69 | os.getenv("GOOGLE_CLIENT_ID") | (none) | Yes |
| GOOGLE_CLIENT_SECRET | calendar_integration.py:70 | os.getenv("GOOGLE_CLIENT_SECRET") | (none) | Yes |
| GOOGLE_TOKEN | calendar_integration.py:36 | os.getenv("GOOGLE_TOKEN") | (none) | Yes — obtained via /auth/google OAuth flow |
| GOOGLE_DRIVE_FOLDER_ID | drive_integration.py:43, main.py:660 | os.getenv("GOOGLE_DRIVE_FOLDER_ID") | (none — falls back to Drive root) | No — needed for Drive sync |
| GOOGLE_DRIVE_NOTES_FOLDER_ID | note_tidy.py:92 (NOTES_FOLDER_ENV) | os.getenv("GOOGLE_DRIVE_NOTES_FOLDER_ID") | (none) | No — needed for note tidy feature |
| PORT | main.py:868 | os.getenv("PORT", 8000) | 8000 | No (Railway sets automatically) |
| RAILWAY_PUBLIC_DOMAIN | main.py:53 | os.getenv("RAILWAY_PUBLIC_DOMAIN", "http://localhost:8000") | http://localhost:8000 | Yes for production |

App facts:
- Entry point: main.py (FastAPI, uvicorn)
- Local run: uvicorn main:app --reload (default port 8000)
- Deployment: Railway (Procfile or railway.toml present)
- Python: see runtime.txt
- Deps: requirements.txt
- Frontend: static/index.html (served at /)
- Auth: password overlay on first load; enter API_KEY value
- Google OAuth: visit /auth/google, follow flow, copy GOOGLE_TOKEN from Railway logs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md</name>
  <files>README.md</files>

  <read_first>
    - main.py — lines 47, 53-59, 868 — confirm env var names, defaults, and which routes they affect
    - .planning/ROADMAP.md — Phase 3 success criteria lists the exact 11 vars that must be covered
    - .planning/PROJECT.md — project description and context for the intro section
    - runtime.txt — Python version to include in quick-start
    - requirements.txt — to confirm pip install step is correct
  </read_first>

  <action>
Create README.md at the repository root with the following content. Write every section as specified. Do not add sections not listed here. Do not omit any env var row from the table.

---

```markdown
# Midwife Agent

A clinical support assistant for a Licensed Maternity Carer (LMC) midwifery practice in Northland, New Zealand.

The agent connects to Google Calendar, Gmail, and Google Drive so the midwife can check availability, book appointments, draft emails, and tidy de-identified notes — through a chat interface, without switching between apps.

Deployed on [Railway](https://railway.app). Backend: FastAPI + Anthropic Claude. Frontend: `static/index.html`.

---

## Quick start (local)

**Prerequisites:** Python 3.11+, a Google Cloud project with OAuth 2.0 credentials, an Anthropic API key.

```bash
# 1. Clone and install dependencies
git clone <repo-url>
cd midwife-agent
pip install -r requirements.txt

# 2. Create .env with required vars (see Environment Variables below)
cp .env.example .env   # or create .env manually
# Edit .env and fill in API_KEY, ANTHROPIC_API_KEY, GOOGLE_CLIENT_ID,
# GOOGLE_CLIENT_SECRET. Leave PORT and RAILWAY_PUBLIC_DOMAIN unset for local dev.

# 3. Start the server
uvicorn main:app --reload

# 4. Open the app
# Visit http://localhost:8000 in your browser.
# Enter your API_KEY value as the password when prompted.

# 5. Connect Google (first run)
# Visit http://localhost:8000/auth/google
# Complete the OAuth consent screen.
# The token will be printed in your terminal (search for GOOGLE_TOKEN).
# Copy the JSON value into GOOGLE_TOKEN in your .env file, then restart.
```

---

## Deployment (Railway)

1. Push the repo to GitHub and connect it to a Railway project.
2. Set all required environment variables in Railway → Variables (see table below).
3. Railway will detect the `Procfile` or `railway.toml` and start the service automatically.
4. Run the Google OAuth flow once after first deploy: visit `https://<your-domain>/auth/google`, complete consent, then retrieve `GOOGLE_TOKEN` from Railway → Deployments → Logs (search `GOOGLE_TOKEN`).

---

## Environment variables

All variables are read at startup. Variables without a default are required — the application will fail or return errors if they are missing.

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| `API_KEY` | **Yes** | *(none — fail-closed)* | Shared password for the deployment. All `/api/*` routes return HTTP 401 if this is unset or if the request header does not match. Choose a memorable phrase (e.g. `MidwifePractice2026!`). See [SECURITY.md](./SECURITY.md) for the full auth model. |
| `ANTHROPIC_API_KEY` | **Yes** | *(none)* | Anthropic API key for Claude. Obtain from [console.anthropic.com](https://console.anthropic.com). Used by the chat endpoint, email watcher, and note tidy. |
| `GOOGLE_CLIENT_ID` | **Yes** | *(none)* | OAuth 2.0 client ID from Google Cloud Console → APIs & Services → Credentials. |
| `GOOGLE_CLIENT_SECRET` | **Yes** | *(none)* | OAuth 2.0 client secret for the same credential. |
| `GOOGLE_TOKEN` | **Yes** | *(none)* | JSON token obtained by completing the `/auth/google` OAuth flow. On Railway, retrieve from Deployments → Logs after the flow (search `GOOGLE_TOKEN`). See [SECURITY.md § OAuth Token Handling](./SECURITY.md#3-oauth-token-handling). |
| `ALLOWED_ORIGIN` | **Yes (prod)** | `http://localhost:8000` | Full HTTPS URL of the deployment, used for CORS. Example: `https://midwife-agent-production.up.railway.app`. Must match the browser origin exactly — no trailing slash. Not needed for local development. |
| `RAILWAY_PUBLIC_DOMAIN` | **Yes (prod)** | `http://localhost:8000` | Public URL of the Railway deployment. Used to construct the OAuth redirect URI. Set automatically by Railway if you use the `${{RAILWAY_PUBLIC_DOMAIN}}` reference, or set it manually as the full domain (with or without `https://` — the app normalises it). |
| `CLAUDE_MODEL` | No | `claude-sonnet-4-5` | Anthropic model string passed to every `client.messages.create` call. Override to pin a specific model version. Example: `claude-opus-4-5`. |
| `GOOGLE_DRIVE_FOLDER_ID` | No | *(Drive root)* | Google Drive folder ID for the knowledge base sync (`/api/drive/sync`). If unset, the sync falls back to the root of My Drive. Find the ID in the Drive folder URL: `drive.google.com/drive/folders/<ID>`. |
| `GOOGLE_DRIVE_NOTES_FOLDER_ID` | No | *(none)* | Google Drive folder ID for the note tidy feature. The `tidy_note_from_drive` tool lists files from this folder. If unset, note tidying from Drive is unavailable (pasted notes still work). |
| `PORT` | No | `8000` | Port uvicorn listens on. Set automatically by Railway — do not set manually in Railway Variables. |

---

## Google OAuth setup

1. Create a project in [Google Cloud Console](https://console.cloud.google.com).
2. Enable the **Gmail API**, **Google Calendar API**, and **Google Drive API**.
3. Create an **OAuth 2.0 Client ID** (type: Web application).
4. Add the following to **Authorised redirect URIs**:
   - `http://localhost:8000/auth/google/callback` (local dev)
   - `https://<your-railway-domain>/auth/google/callback` (production)
5. Download the credentials and copy `Client ID` → `GOOGLE_CLIENT_ID`, `Client Secret` → `GOOGLE_CLIENT_SECRET`.
6. After deploying, run the OAuth flow once to obtain `GOOGLE_TOKEN` (see Quick start step 5).

---

## Key files

| File | Purpose |
|------|---------|
| `main.py` | FastAPI app, all routes, tool-use loop, auth middleware |
| `calendar_integration.py` | Google Calendar CRUD + OAuth credential management |
| `gmail_integration.py` | Gmail read, draft, search |
| `drive_integration.py` | Google Drive folder sync into the RAG knowledge base |
| `email_watcher.py` | Background thread: polls Gmail every 120 s, auto-drafts replies |
| `note_tidy.py` | Note formatting via Claude (Drive folder or pasted text) |
| `static/index.html` | Frontend chat UI (canonical — served at `/`) |
| `SECURITY.md` | Auth model, CORS policy, OAuth token handling, credential rotation |

---

## Security

See [SECURITY.md](./SECURITY.md) for:
- Bearer token auth model and exempt routes
- CORS policy configuration
- OAuth token retrieval from Railway Logs
- Step-by-step credential rotation runbook
```

---

Write this file verbatim to `README.md` at the repository root. Preserve all headings, table formatting, code blocks, and blank lines exactly as shown. Do not add, remove, or reword any content.
  </action>

  <verify>
    <automated>grep -c "API_KEY" README.md && grep -c "ALLOWED_ORIGIN" README.md && grep -c "CLAUDE_MODEL" README.md && grep -c "ANTHROPIC_API_KEY" README.md && grep -c "GOOGLE_CLIENT_ID" README.md && grep -c "GOOGLE_CLIENT_SECRET" README.md && grep -c "GOOGLE_TOKEN" README.md && grep -c "GOOGLE_DRIVE_FOLDER_ID" README.md && grep -c "GOOGLE_DRIVE_NOTES_FOLDER_ID" README.md && grep -c "PORT" README.md && grep -c "RAILWAY_PUBLIC_DOMAIN" README.md</automated>
  </verify>

  <acceptance_criteria>
    - README.md exists at repo root: `test -f README.md`
    - All 11 required vars present in the file (each grep returns >= 1):
      - `grep -c "API_KEY" README.md`
      - `grep -c "ALLOWED_ORIGIN" README.md`
      - `grep -c "CLAUDE_MODEL" README.md`
      - `grep -c "ANTHROPIC_API_KEY" README.md`
      - `grep -c "GOOGLE_CLIENT_ID" README.md`
      - `grep -c "GOOGLE_CLIENT_SECRET" README.md`
      - `grep -c "GOOGLE_TOKEN" README.md`
      - `grep -c "GOOGLE_DRIVE_FOLDER_ID" README.md`
      - `grep -c "GOOGLE_DRIVE_NOTES_FOLDER_ID" README.md`
      - `grep -c "PORT" README.md`
      - `grep -c "RAILWAY_PUBLIC_DOMAIN" README.md`
    - Contains quick-start section: `grep -c "Quick start\|quick start" README.md` returns >= 1
    - Contains env var table: `grep -c "| Variable" README.md` returns >= 1
    - References SECURITY.md: `grep -c "SECURITY.md" README.md` returns >= 1
    - No real credentials embedded: `grep -i "client_secret\s*=" README.md` returns no output
  </acceptance_criteria>

  <done>README.md exists at repo root. The env var table contains all 11 required vars (API_KEY, ALLOWED_ORIGIN, CLAUDE_MODEL, ANTHROPIC_API_KEY, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_TOKEN, GOOGLE_DRIVE_FOLDER_ID, GOOGLE_DRIVE_NOTES_FOLDER_ID, PORT, RAILWAY_PUBLIC_DOMAIN). A quick-start section explains local dev and Railway deployment. SECURITY.md is cross-referenced for auth details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem write | Plan writes README.md to repo root — no untrusted input, static content only |
| Documentation accuracy | README.md describes env vars; inaccurate descriptions lead operators to misconfigure the deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | README.md env var table | mitigate | Use placeholder examples only (e.g. `MidwifePractice2026!`); do not embed real API keys, tokens, or client secrets in documentation |
| T-03-02-02 | Tampering | README accuracy for GOOGLE_TOKEN | mitigate | Executor reads main.py and calendar_integration.py before writing — token description grounded in actual implementation (Railway Logs path), not assumption |
| T-03-02-03 | Elevation of Privilege | Misleading ALLOWED_ORIGIN documentation | mitigate | Documentation must state "no trailing slash" and "no wildcard" to prevent operators from misconfiguring CORS to be overly permissive |
| T-03-02-04 | Information Disclosure | Google OAuth setup section | accept | Section describes process steps, not credentials; operators supply their own credentials — no secrets embedded |
</threat_model>

<verification>
After task completes:

```bash
# File exists
test -f README.md && echo "EXISTS"

# All 11 vars present
for var in API_KEY ALLOWED_ORIGIN CLAUDE_MODEL ANTHROPIC_API_KEY GOOGLE_CLIENT_ID \
           GOOGLE_CLIENT_SECRET GOOGLE_TOKEN GOOGLE_DRIVE_FOLDER_ID \
           GOOGLE_DRIVE_NOTES_FOLDER_ID PORT RAILWAY_PUBLIC_DOMAIN; do
  count=$(grep -c "$var" README.md 2>/dev/null || echo 0)
  echo "$var: $count"
done

# No credentials embedded
grep -i "client_secret\s*=" README.md || echo "clean"
```
</verification>

<success_criteria>
README.md exists at repo root and:
- Env var table covers all 11 required vars (each row includes: var name, required/optional, default, description with where to find the value)
- Quick-start covers local dev (uvicorn, .env, browser URL, OAuth flow)
- Deployment section covers Railway setup including GOOGLE_TOKEN retrieval from logs
- SECURITY.md cross-referenced for auth details
- No real credentials or token values embedded
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation/03-02-SUMMARY.md` using the summary template.
</output>
